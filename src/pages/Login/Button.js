import React from 'react'

const Button = () => {
  return (
    <input
        type="submit"
        value="Log In"
        className="btn btn-block btn-primary mt-3"
    />
  )
}

export default Button